// I didn't dive much to adding environment variables, as there are so many ways to do it
const config = {
  apiEndpoint: "https://63c91b0f904f040a96562ed6.mockapi.io",
  environment: process.env.NODE_ENV,
};

export default config;
